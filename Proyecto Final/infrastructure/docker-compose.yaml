version: '3.9'

services:

  postgres-db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ud_admin
      POSTGRES_PASSWORD: HDjZLf03nK69O6IGs4Rg
      POSTGRES_DB: ud_DBFoundations_project
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - local

volumes:
  postgres-data:
    driver: local

networks:
  local:
    driver: bridge